<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
    
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ $1 [L]
</IfModule>

Options -Indexes

<FilesMatch "\.(php)$">
    Order allow,deny
    Allow from all
</FilesMatch>
